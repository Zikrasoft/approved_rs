1. we have to add checkboxes with services on the autoservice/detailing pages.
2. use ZOD for whole project
3. Давай в эту же ветку добавим заешь что, нужен CTA в начале или конце или в сережине кейсов, и cta баннер должен хорошо вписываться туда и подходить под выбранный флиььтр
4. Баг: у заявки #2 после postpone в боте статус "Отложена", а карточка в группе осталась "New" (не обновилась) — при "В работу" группа обновляется нормально. Проверить на новых заявках: обновляется ли групповая карточка вообще (возможно, старый баг ещё с blob/notifyLead, не только postpone-код).
5. Добавить наши соцсети
6. Astro experimental incremental builds (появилось в 7.2.0) — пропускает ре-рендер статичных страниц, если контент и код не менялись (cacheKey + хэш графа зависимостей). Потенциально полезно т.к. translate-cases.yml теперь коммитит в main на каждый перевод кейса → полный ребилд на Vercel каждый раз. НО: experimental, нужно ретрофитить cacheKey на каждый getStaticPaths() entry (у нас через withLocales — много мест), любое изменение layout/компонента сбрасывает кэш целиком, build.concurrency надо ставить ≤1. Возвращаться к этому когда сайт стабилизируется и поток коммитов — в основном переводы, а не правки UI.
7. Сделать что бы весь src/i18n переводился автоматом через AI
8. Языки писать прямо на самом языке без сокращения
9. Переделать систему при которой меню закрывается на главной в мобильной. Ндао что бы оно чуть дольше не закрывлось при скролле fabel 5 запустить
10. Рассмотреть telegraf (или grammy) вместо ручных fetch-вызовов в src/lib/telegram/client.ts — сейчас это несколько исходящих notify-вызовов (sendMessage/editMessageText/answerCallbackQuery), не интерактивный бот-луп, так что фреймворк — это over-engineering для текущего объёма. Вернуться к этому, если бот обрастёт более сложным диалоговым сценарием.
