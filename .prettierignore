# Prettier already reads .gitignore by default (dist/.astro/.vercel/node_modules/graphify-out) —
# this file only needs entries .gitignore doesn't cover.
pnpm-lock.yaml

# Admin-authored case-study content (Keystatic-managed Markdown, not code) —
# prettier reformatting these isn't wanted and shouldn't gate CI.
apps/approved-rs/src/content/cases/**/*.md
apps/approved-rs/src/content/autoservice-cases/**/*.md
apps/approved-rs/src/content/detailing-cases/**/*.md
