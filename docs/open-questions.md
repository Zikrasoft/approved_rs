# Открытые вопросы

Копятся по ходу работы над разделением портфеля на независимые бренды.
Стратегические вопросы — в плане (`~/.claude/plans/encapsulated-whistling-melody.md`),
здесь только то, что всплыло при реализации и требует решения владельца/клиента.

## Блокирующие запуск

### 1. Имена и домены двух новых брендов

Сайты сделаны на заглушках: **PRIZMA** (`prizma.rs`) для детейлинга и
**AUTOHUB** (`autohub.rs`) для автосервиса. Нужны финальные названия и домены.

Правило из плана: **общего корня бренда быть не должно** — пара с общим корнем
читается как один бизнес с двумя вывесками и обнуляет смысл разделения. Также
избегать семантики Approved (доверие / проверка / certified / verified /
trusted).

Замена названия — это правки в:

- `apps/<app>/src/utils/constants.ts` (`SITE_NAME`, `SITE_LEGAL_NAME`, адрес),
- `apps/<app>/src/lib/crm.ts` (`BRAND` — метка бренда в карточке Telegram),
- `apps/<app>/astro.config.mjs` и `vercel.json` (`site` / `SITE`),
- `apps/<app>/src/content/i18n/*.yaml` (название встречается в мета-тегах),
- `apps/<app>/src/components/Wordmark.astro` (логотип),
- `apps/<app>/scripts/gen-og.mjs`, затем `pnpm --filter … gen:og`.

`BRAND` меняется только для новых заявок: в уже сохранённых лидах остаётся то
значение, с которым они были созданы — это сделано намеренно, так же как со
ставкой комиссии.

### 2. Реальные адреса и телефоны

Сейчас заглушки: `Vojvode Stepe 142` / `+381 60 000 0000` у детейлинга,
`Zrenjaninski put 84` / `+381 11 000 0000` у автосервиса. Адрес уходит в
schema.org (`AutoDetailing` / `AutoRepair`) и в карту на странице контактов,
телефон — в кнопку звонка и в футер.

По плану у трёх бизнесов должны быть **три разных физических адреса**: общий
адрес в Google Maps связывает бренды сильнее любых других следов.

### 3. Фотографии работ

Обе галереи пустые, и это сознательно: выдумывать портфолио нельзя. Пока
работ нет, страница показывает честную заглушку («скоро появятся, напишите
нам»), а на главной пропадает блок «до/после».

Нужны реальные фото. Для детейлинга у карточки работы есть отдельное поле
«фото до» — если его заполнить, на странице работы и на главной включается
слайдер сравнения.

### 4. Каталог аккумуляторов

Магазин собран и проверен, но товаров нет — цены и наличие выдумывать нельзя.
Заводится через Keystatic (`Аккумуляторы`): бренд, цена, ёмкость, пусковой ток,
полярность, габариты, гарантия, наличие, фото и список совместимости
(марка / модель / годы).

Пока каталог пуст, страница показывает заглушку со ссылкой на звонок.

### 5. Цены на детейлинг

В `home.yaml` стоят реальные рыночные вилки по Белграду (PPF от 690 €, полировка
с керамикой от 450 €, смена цвета от 1 900 €), но это **мои оценки, не ваши
цены**. Проверьте перед запуском — они видны на главной и на страницах услуг.

## Требуют решения до запуска магазина

### 6. Источник данных совместимости аккумуляторов

Сейчас совместимость вбивается руками на карточке товара: список
`марка / модель / год с / год по`. Из этого на сборке собирается индекс, и
фильтр на клиенте работает по нему.

Для десятка ходовых моделей сербского рынка этого достаточно и это самый
дешёвый вариант. Если каталог вырастет до сотен позиций, понадобится внешняя
база (TecDoc и аналоги, платная лицензия) и серверный запрос вместо
клиентского фильтра. Решать по факту роста, не заранее.

### 7. Ставки комиссии по брендам

Сейчас в конфиге: автоподбор 10%, детейлинг 20%, автосервис 15%. Первая цифра
из истории, вторая и третья — мои. Из разговора звучало «детейлинг ~50%».
Поправьте `DEFAULT_COMMISSION_PERCENT` в `apps/<app>/src/lib/crm.ts`.

Переопределение на уровне заявки работает в боте в любом случае, вопрос только
про дефолт для новых заявок.

## Технические решения, принятые без владельца

### Medusa не заводилась

План предполагал Medusa для магазина. По факту на старте нужен каталог, корзина
и заказ как заявка — без онлайн-оплаты. Это ~200 строк: корзина в localStorage,
состав заказа уходит в комментарий заявки, оплата в сервисе при получении.

Medusa начинает окупаться, когда появятся остатки на складе, онлайн-оплата или
второй канал продаж. Тогда меняется только источник данных о товарах — верстка
каталога и корзина остаются. Вопрос хостинга Medusa (Railway / Render / Fly /
Hetzner) до этого момента не встаёт.

### Один Telegram-бот, один стор, поле бренда

План предлагал разные ключи хранилища на бизнес. Telegram разрешает **один
webhook на бота**, поэтому бот физически живёт в одном приложении
(`apps/approved-rs`) и должен видеть лиды всех трёх брендов. Разные файлы
потребовали бы реестра сторов и префиксов в каждом `callback_data`.

Сделано проще: один `data/leads.json`, у лида есть поле `brand`, оно
проставляется приложением и не приходит от посетителя. В карточке, в списках
и в статистике бренд виден; лиды разных брендов не склеиваются между собой.
Разделить на отдельные файлы позже — это миграция одного JSON.

### `packages/ui-kit` не появился, появился `packages/site-kit`

Выносить визуальные примитивы нельзя — это ровно то, по чему два сайта
опознаются как родственники. Вынесена только механика, у которой к тому
моменту стало три потребителя: `safeMarkdown` (граница XSS для
автопереводов), `formatPhone`, идентификатор посетителя.

### Базовый `tsconfig` в корне — по-прежнему не нужен

`astro/tsconfigs/strict` даёт весь строгий набор, а `paths` резолвятся
относительно своего файла и из корня указывали бы не туда. Три приложения это
подтвердили: общего, кроме одной строки `extends`, не набралось.

## Что нужно завести в инфраструктуре

Перед первым деплоем новых сайтов:

1. **Два проекта на Vercel** — на `apps/detailing` и `apps/auto-service`.
2. **Секреты в GitHub Actions**: `VERCEL_PROJECT_ID_DETAILING` и
   `VERCEL_PROJECT_ID_AUTO_SERVICE`. Пока их нет, соответствующие деплой-джобы
   просто пропускаются, CI от этого не краснеет.
3. **Тот же Vercel Blob store** подключить ко всем трём проектам — лиды лежат
   в одном `data/leads.json`.
4. **Те же `TELEGRAM_*`** во всех трёх проектах: бот один.
5. **Домены** — после ответа на вопрос 1.

## Состояние на утро 12.09.2026

Все четыре фазы плана закрыты.

| PR  | Куда            | Что                                                        |
| --- | --------------- | ---------------------------------------------------------- |
| #14 | `main`          | Переход на монорепо, approved.rs → `apps/approved-rs`      |
| #15 | `feature/split` | `packages/lead-crm`                                        |
| #16 | #15             | `packages/i18n`                                            |
| #17 | #16             | Актуализация `CLAUDE.md`                                   |
| #18 | #17             | Единый источник списка локалей                             |
| #19 | #18             | Статус в `docs/open-questions.md`                          |
| #20 | #19             | Мультибрендовость CRM + роуты заявок в пакете              |
| #21 | #20             | Автоперевод вынесен в `@podbor/i18n`, починен бандл Vercel |
| #22 | #21             | Сайт детейлинга PRIZMA + `packages/site-kit`               |
| #23 | #22             | Английские URL на детейлинге                               |
| #24 | #23             | Сайт автосервиса AUTOHUB с магазином                       |

Мержить снизу вверх: #14 в `main` первым — пока он не влит, любой хотфикс в
`main` пишется по старым путям и конфликтует с переименованиями. Дальше
#15 → #24 по цепочке, GitHub сам переставит базы.

### Что сделано сверх плана, по итогам `review-local`

Фаза 2:

- Заявка с `service=constructor` сохранялась, но не долетала в Telegram, а её
  карточка навсегда переставала открываться.
- Импорт стора требовал Telegram-переменных и мог уронить сборку.
- Корневой `vercel.json` вернул `git.deploymentEnabled: false` — после переезда
  файла в приложение Vercel перестал его видеть и начал собирать превью на
  каждую ветку.

Фазы 3–4:

- **В функцию Vercel уезжал весь репозиторий**, включая `.git` и `.env*`:
  dev-ветка админских эндпоинтов переживала сборку, и `@vercel/nft` разворачивал
  `process.cwd()` в «все файлы под корнем воркспейса». 353 МБ → 32 МБ.
- **`/` отдавал бы 500 в проде** на обоих новых сайтах: Astro запрещает
  `rewrite` с on-demand роута на пререндеренный. Заменено на `redirect`.
- **Локаль терялась при отправке формы**: middleware не выполняется для
  пререндеренных страниц на Vercel, поэтому куки `lang` могло не быть, и любой
  не-русский посетитель попадал на `/ru/thanks/`, а лид сохранялся как `ru`.
  Форма теперь несёт локаль сама. Это чинит и approved.rs.
- **Keystatic в проде писал не туда**: пути GitHub API резолвятся от корня
  репозитория, а после переезда в монорепо контент лежит в `apps/<app>/`.
  Это регрессия от PR #14, которую поймало только ревью нового сайта.
- Публичные эндпоинты заявок получили ограничения длины полей: без них любой
  мог раздувать общий `data/leads.json` и положить приём заявок у всех трёх
  сайтов сразу.
- Мелочи: дубли `id` в форме (три формы на странице, любая подпись фокусировала
  первую), 404 на русском для sr/en, `sr-RS` вместо `sr-Latn-RS` (даты
  печатались кириллицей на латинском сайте), `noindex`-страницы в sitemap,
  грамматика счётчика («2 подходящих аккумуляторов» → «аккумулятора»).

Пакеты покрыты тестами на 100% (statements/functions/lines), порог проверяет
сам `test`-скрипт, так что CI падает при просадке. Итого 863 теста.
