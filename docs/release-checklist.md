# Чеклист перед релизом

Состояние на 2026-09-14, ветка `feature/split`. Отмечать по мере закрытия.

## Блокеры — без этого выкатывать нельзя

- [ ] **Переменные окружения для трёх проектов Vercel.** Полная таблица с
      последствиями каждого пропуска — в `docs/deploy.md`. Отдельно проверить
      заглушки: `PUBLIC_INSTAGRAM` по умолчанию `details.studio`,
      `PUBLIC_PHONE_NUMBER` — номер-заглушка в `constants.ts`. Оба уедут в прод
      молча.
- [ ] **Один Blob store подключить ко всем трём проектам.** Лиды всех брендов
      живут в общем `data/leads.json` и разделяются полем `brand`.
- [ ] **Telegram webhook.** Один URL на бота, ставится только на approved.rs.
      Нужен `TELEGRAM_WEBHOOK_SECRET`, иначе `/api/telegram-webhook` отвечает
      401 и кнопки бота мертвы.
- [ ] **`CRON_SECRET`** — без него `/api/reminders` отвечает 401 и напоминания
      по отложенным заявкам не уходят никогда.
- [ ] **`OPENAI_API_KEY` в GitHub Secrets**, не в Vercel. Без него джоба
      `translate` падает, а `verify` и `deploy` висят на ней.
- [ ] **Домены и Root Directory.** `details.rs`, `carlab.rs`, три проекта с
      тремя разными корнями.
- [ ] **Политика конфиденциальности противоречит поведению сайта.**
      `pages.yaml` обещает аналитические cookies «только после вашего
      согласия» на пяти языках, а GA и Метрика теперь стартуют на первой
      загрузке и держит их только явный отказ. Плюс Метрика поднимается с
      `webvisor: true` — это запись сессии, включая ввод в поля формы. Нужно
      либо переписать ru-текст политики под реальность (остальные локали
      доберёт CI), либо вернуть гейт. Отдельно учесть: отказ вступает в силу
      со следующей загрузки — выгрузить уже запущенный счётчик Метрики нечем.

- [ ] **Первый пуш прогоняет перевод.** `site.yaml` обоих новых сайтов сейчас с
      устаревшим `translatedFrom` — добавлен блок `cookie`. en/sr придут из CI;
      до этого баннер на них покажет русский текст.

## Контент — это владелец

- [ ] **Кейсы под четыре страницы услуг без доказательств:** у Details
      полировка/керамика и реставрация руля, у CarLab кузовной ремонт и
      проверка перед покупкой. Самые маржинальные страницы, и на них нечего
      показать.
- [ ] **Кейс GLS у Details** опубликован с текстом «результат покажем, когда
      закончим» и пустой галереей.
- [ ] **Непроверенные утверждения:** гарантия 12 месяцев (на работы или и на
      запчасти?), «ответ за 2 часа», «круглосуточно без выходных», доли
      восстановленных машин по странам.
- [ ] **Цены CarLab в динарах** — решение отложено до 2026-12-01. Поля
      `priceFrom` на месте, вернуть значения и одну строку в двух компонентах.
- [ ] **`SHOP_ENABLED = false`** — решить, запускаем ли магазин. Пока `/shop` и
      `/cart` не собираются, а `/shop`→301→404.

## Техдолг, который стоит закрыть до релиза

- [ ] **Дубли `id` в лидформе approved.rs.** Три `id="contact-telegram"` на
      `/ru/vehicle-sourcing/` — форма рендерится на странице, в шапке и в
      модалке. JS работает (селекторы scoped), но разметка невалидна и
      скринридер путает поля.
- [ ] **Вес контактных страниц новых сайтов.** 490 server-rendered `<option>`
      на страницу плюс вторая полная копия формы в закрытом `<dialog>`:
      ~26 КБ gzip там, где хватило бы ~7. Список стран `bindPhoneCountry` может
      набить из чанка, который и так грузит динамически.
- [ ] **CI сцепляет три бренда.** Один шаг `translate` на все приложения: один
      неисправимый файл у детейлинга останавливает деплой approved.rs.
- [ ] **Гонка `get`+`head` в Blob CAS** (`packages/lead-crm/src/storage/vercelBlob.ts`).
      Запись между двумя вызовами даёт etag от чужой версии, и `ifMatch`
      проходит против контента, который читатель не видел. Нужна проверка
      поведения etag на проде.

## После выкатки

- [ ] Проверить на живом мобильном, что баннер согласия не перекрывает
      плавающую кнопку связи на всех трёх сайтах.
- [ ] Проверить 301 legacy-слагов на проде: `vercel.json` дублирует
      `middleware.ts` руками, и расходятся они молча.
- [ ] Убедиться, что аналитика approved.rs стартует и что отказ в баннере её
      действительно не запускает на следующей загрузке.
